import React from "react";

const ClassCard = ({ singleClass }) => {
  const {
    className,
    classImage,
    classPrice,
    availableSeats,
    musicInstructorName,
  } = singleClass;
  return (
    <div>
      <h2>card</h2>
    </div>
  );
};

export default ClassCard;
